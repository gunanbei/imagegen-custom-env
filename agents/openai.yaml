interface:
  display_name: "Image Gen Custom Env"
  short_description: "Bundled Image Gen with custom credentials"
  default_prompt: "Use $imagegen-custom-env to resolve custom image API credentials and run only the bundled scripts/image_gen.py."
policy:
  allow_implicit_invocation: true
