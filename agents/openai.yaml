interface:
  display_name: "Image Gen Custom Env"
  short_description: "Official Image Gen with custom credentials"
  default_prompt: "Use $imagegen-custom-env to resolve and inject custom image API credentials, then let the official Image Gen skill handle the request."
policy:
  allow_implicit_invocation: true
